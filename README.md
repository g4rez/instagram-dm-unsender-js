"""Instagram DM Unsender.

This script logs into Instagram using instagrapi, lets you select one DM thread (or all threads), scans messages in the selected scope, and attempts to unsend (delete) messages that were sent from your own account.

It stores a session file to reduce repeated logins and provides a console menu for listing threads, viewing recent messages, scanning message history, and deleting eligible messages. """
<img width="1375" height="895" alt="image" src="https://github.com/user-attachments/assets/fdbd9c4a-1bf6-41d9-b015-ab9568ea3bed" />
